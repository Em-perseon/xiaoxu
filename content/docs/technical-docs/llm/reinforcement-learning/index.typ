#import "../../../../../config.typ": template, tufted

#show: template.with(
  title: "强化学习入门",
)

#html.elem("a", attrs: (class: "back-link", href: "/xiaoxu/docs/technical-docs/llm/"))[← 返回大语言模型的漫游指南]

#heading(level: 1, outlined: false)[强化学习入门]

#outline(title: [目录], depth: 3)

== 基础术语

*环境（environment）*：智能体操作的对象。智能体在环境中执行动作，并从环境中获得奖励或惩罚。环境定义了智能体的状态空间、动作空间和反馈机制。

*智能体（agent）*：执行动作并与环境交互的实体。它通过学习选择动作，以最大化长期回报。

*状态（state）*：环境在某一时刻的描述。状态包含智能体做决策所需的信息，可以是离散的，也可以是连续的。

*动作（action）*：智能体在某一状态下选择的行为。动作同样可以是离散的或连续的。

*轨迹（trajectory）*：由一系列状态、动作和奖励组成的交互序列：

$ tau = (s_1, a_1, r_1, s_2, a_2, r_2, dots) $

*策略（policy）*：智能体在各个状态下选择动作的规则。策略既可以是确定性的，也可以给出动作的概率分布：

$ pi(a | s) = P(A = a | S = s) $

*奖励（reward）*：智能体执行动作后从环境获得的即时反馈，通常是一个数值。

*回报（return）*：从时刻 $t$ 开始可以获得的未来奖励总和：

$ U_t = R_t + R_(t+1) + R_(t+2) + dots $

为了降低遥远奖励的权重，可以引入折扣因子 $gamma$。折扣回报为：

$ U_t = R_t + gamma R_(t+1) + gamma^2 R_(t+2) + gamma^3 R_(t+3) + dots $

回报是智能体与环境实际交互后累积得到的数值。

*价值函数（value function）*：用于评估状态或“状态—动作”组合的好坏程度。

动作价值函数表示：在状态 $s_t$ 下执行动作 $a_t$，并继续遵循策略 $pi$ 时，折扣回报的期望：

$ Q_pi(s_t, a_t) = E[U_t | S_t = s_t, A_t = a_t] $

其中，动作由策略决定，状态由环境的转移规律决定：

$ P(A = a | S = s) = pi(a | s) $

$ P(S' = s' | S = s, A = a) = p(s' | s, a) $

状态价值函数表示：在状态 $s$ 下按照策略 $pi$ 行动时的期望回报。对于离散动作，可以写成：

$ V_pi(s) = E_(A ~ pi(dot | s))[Q_pi(s, A)] = sum_a pi(a | s) Q_pi(s, a) $

*优势函数（advantage function）*：衡量某个动作相对于当前状态下平均表现的额外收益：

$ A_pi(s, a) = Q_pi(s, a) - V_pi(s) $

其中，$Q_pi(s,a)$ 评估特定动作的期望回报，$V_pi(s)$ 评估当前策略在该状态下的平均回报。

== 方法概览

在 Actor-Critic 框架中，*行动者（Actor）*是负责选择动作的策略网络，*评论家（Critic）*是负责评价状态或动作的价值网络。Critic 提供“这个动作有多好”的信号，Actor 再根据该信号改进策略。

这种配合有两个主要优点：

1. Critic 能提供方差较低的估计，因此通常比纯策略梯度学习得更快；
2. Critic 可以在轨迹尚未结束时评价当前状态，不必等到整个回合结束后再学习。

=== 基于价值的方法

DQN 属于基于价值的方法。它学习动作价值函数 $Q(s,a)$，并选择价值最大的动作：

$ s -> Q(s, a) -> op("argmax")_a Q(s, a) $

这里没有单独的策略网络，Q 网络本身承担 Critic 的角色。

=== 基于策略的方法

策略梯度直接学习动作的概率分布：

$ s -> pi(a | s) -> a $

它有显式的 Actor，但可以不使用价值网络。优点是能够表达复杂的随机策略；缺点是只依靠奖励评价动作时，梯度方差较大、学习速度较慢。

=== Actor-Critic 方法

Actor-Critic 同时学习策略与价值函数：

$ s -> pi(a | s) -> a $

$ (s, a) -> Q(s, a) $

Actor 负责决策，Critic 负责评价。这种方法兼具策略的表达能力和价值估计的学习效率，但需要同时训练两个网络。

=== 三类方法的区别

*纯 Actor*直接学习策略，不依赖价值函数。它结构简单，可以表示随机或确定性策略，但更新方差较大。

*纯 Critic*直接学习 $Q(s,a)$，策略隐含为选择 Q 值最大的动作。它能够较高效地利用数据，但策略形式受到贪心或 $epsilon$-贪心规则的限制。

*Actor + Critic*由 Actor 表示策略，由 Critic 提供低方差的评价信号。它的学习速度和策略表达能力较强，但实现更复杂。

== 专题文章

- #link("dqn/")[DQN 算法]
- #link("policy-gradient/")[策略梯度]
- #link("actor-critic/")[Actor-Critic 方法]
- #link("ppo/")[近端策略优化（PPO）]
- #link("grpo/")[群组相对策略优化（GRPO）]
