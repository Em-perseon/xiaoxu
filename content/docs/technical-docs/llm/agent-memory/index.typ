#import "../../../../../config.typ": template, tufted

#show: template.with(
  title: "智能体记忆系统",
)

#context {
  if target() == "html" {
    html.elem("a", attrs: (class: "back-link", href: "/xiaoxu/docs/technical-docs/llm/"))[← 返回大语言模型的漫游指南]
  }
}

#heading(level: 1, outlined: false)[智能体记忆系统]

#outline(title: [目录], depth: 3)

#let memory-box(title, body) = context {
  if target() == "html" {
    html.elem("div", attrs: (class: "router-policy-box"))[
      *#title*
      #body
    ]
  } else {
    rect(
      width: 100%,
      fill: luma(96%),
      stroke: 0.6pt + luma(70%),
      radius: 3pt,
      inset: 10pt,
    )[
      *#title*
      #body
    ]
  }
}

== 引言

=== 动机：为什么智能体需要记忆
因为每次输入prompt的上下文窗口(context window)是有限的，也就是模型能够注意到的优先token序列，这是模型生成时唯一可用的信息。对于短小、自包含的任务，这已经足够。但对于长周期的智能体式任务，它却是一个根本瓶颈。

当智能体缺少这种持久性记忆时，会出现三种不同的失败模式：
1. 上下文灾难性遗忘：一旦某个事件滑出上下文窗口，它就会不可挽回地丢失。智能体无法回看10，000个token之前所作出的决策。
2. 无法从经验中学习：没有情节式存储，每个回合都是智能体的第一次。成功策略无法复用，错误会被重复。
3. 缺乏个性化：用户偏好、领域事实和关系历史必须在每个会话中重新建立，既损害用户体验，也拖累效率。

#memory-box("作为认知架构的记忆")[

  认知科学在生物智能体中区分了几类记忆系统：

  - *工作记忆（working memory）：* 对当前信息进行短期保存与主动操作；
  - *情节记忆（episodic memory）：* 保存智能体经历过的具体事件与自传式经验；
  - *语义记忆（semantic memory）：* 保存事实、概念以及关于世界的一般知识；
  - *程序性记忆（procedural memory）：* 保存技能、习惯和可以反复执行的操作方式。

  有效的智能体式 AI 系统同样能从这种区分中受益。这并不是因为需要模拟神经科学，而是因为这些类别对应着真正不同的信息访问模式、更新频率和检索机制。
]

形式化地，可以将带有记忆系统的智能体表示为四元组 $cal(A) = (pi_theta, cal(M), cal(R), cal(W))$。其中，$pi_theta$ 是参数为 $theta$ 的智能体策略（通常由 LLM 实现），$cal(M)$ 是记忆存储，$cal(R): cal(Q) times cal(M) arrow.r cal(D)$ 是将查询映射为相关文档或记忆的检索函数，$cal(W): cal(M) times cal(E) arrow.r cal(M)$ 是利用新经验 $cal(E)$ 更新记忆的写入函数。

在每个时间步 $t$，智能体首先观察到 $o_t$，再从记忆中检索相关上下文 $c_t = cal(R)(o_t, cal(M))$，随后根据系统提示、检索记忆和近期上下文历史选择动作：

$
  a_t
  ∼
  pi_theta(· | [s_t; c_t; h_t])
$

其中，$s_t$ 表示当前系统 prompt，$c_t$ 表示从记忆系统中检索到的内容，$h_t$ 表示近期上下文历史。执行动作并获得结果或奖励 $r_t$ 后，智能体将这次经验写回记忆：

$ cal(M) ← cal(W)(cal(M), (o_t, a_t, r_t)) $

== 记忆类型分类

#figure(caption: ":智能体记忆系统的四分法分类，对应认知科学中的区分。每种记忆类型都有不同的访问模式、更新
频率和检索机制。")[
  #image("memory-types.png", width: 75%)
]

=== 工作记忆（短期）

工作记忆是智能体的活动工作区，也就是当前正在被操作的信息。在 LLM 智能体中，它对应于：

- *草稿区：* 在生成最终答案之前，写入专用缓冲区的中间推理步骤，例如思维链（CoT）或 scratchpad；
- *思维链缓冲区：* 在答案 token $a$ 之前生成的推理 token 序列 $z_1, z_2, dots, z_k$，可以形式化为：

$ p(a | x) = sum_z p(a | x, z) p(z | x) $

- *对话上下文：* 保存在上下文窗口中的近期轮次历史 $[(u_1, a_1), dots, (u_t, a_t)]$。

工作记忆的访问速度快，因为内容已经位于上下文中；但它容易丢失，一旦上下文窗口被新的 token 填满，较早的信息就会滑出。它的容量也受到上下文长度限制。

=== 情节记忆（基于经验）

情节记忆存储按时间索引的具体过去事件。对于智能体而言，它可以记录：

- *过去交互：* 先前对话、任务尝试及其结果的完整或摘要记录；
- *成功轨迹：* 可作为少样本示例被检索出来，用于相似未来任务的高奖励动作序列；
- *失败案例：* 带有根因注释的已记录错误，使智能体能够避免重复犯错；
- *检索增强的情节回忆：* 给定新任务 $q$，检索最相似的 $k$ 个过去情节 ${e_i}_(i=1)^k$，并将它们放入当前上下文。

情节记忆通常实现为向量存储（见第 17.3.1 节），并对情节摘要进行 embedding，以便根据当前任务检索相似经验。

=== 语义记忆（世界知识）

语义记忆编码与具体事件无关的一般事实和概念：

- *事实性知识：* 实体、属性和关系，例如“巴黎是法国的首都”；
- *领域概念：* 与智能体任务领域相关的定义、分类体系和术语；
- *知识图谱：* 结构化表示为 $cal(G) = (cal(V), cal(E))$，其中节点 $v in cal(V)$ 表示实体，边 $e in cal(E)$ 表示类型化关系。

与情节记忆不同，语义记忆是上下文无关的：例如“水在 $100 degree C$ 沸腾”这一事实，不会因为智能体何时学到它而改变。

=== 程序性记忆（技能）

程序性记忆编码如何做事，也就是已经自动化的技能和动作模式：

- *学到的工具使用模式：* 哪个任务调用哪个 API、如何格式化输入，以及如何处理错误；
- *动作序列：* 多步过程，例如“部署代码：运行测试 → 构建镜像 → 推送 → 更新 manifest”；
- *作为记忆的策略：* 模型参数 $theta$ 本身编码了程序性知识；在成功轨迹上微调是一种程序性记忆巩固。

#memory-box("例子：智能体协助部署一个新服务")[

  假设用户要求智能体“把新版本服务部署到测试环境”。四类记忆会在同一个任务中分工协作：

  - *工作记忆：* 保存当前用户请求、最近的测试输出、已经执行的命令，以及下一步待完成的动作；
  - *情节记忆：* 检索过去相似的部署轨迹，例如上一次因为环境变量缺失而失败的记录，并提醒智能体提前检查；
  - *语义记忆：* 提供部署平台的 API 事实、配置项含义、服务依赖关系和错误码解释；
  - *程序性记忆：* 提供已经学会的操作流程：“运行测试 → 构建镜像 → 推送镜像 → 更新 manifest → 检查状态”。

  智能体完成任务后，再将本次观察、动作和结果写回情节记忆；如果某个操作流程在多次任务中都有效，还可以通过微调或策略更新将其巩固为新的程序性记忆。
]


== 记忆架构

=== 基于 RAG 的记忆

[#link("/xiaoxu/docs/technical-docs/llm/rag/")[检索增强生成（RAG）]] 是 LLM 智能体外部记忆的主导范式。记忆存储 $cal(M)$ 是一组文档 $\{d_i\}_(i=1)^N$，检索函数则把查询 $q$ 映射为一个按相关性排序的文档子集。

*Embedding 存储和向量数据库。* 每个文档 $d_i$ 由 embedding 模型 $phi$ 编码为向量 $v_i = phi(d_i) in RR^D$，查询也以同样方式编码为 $q = phi(q)$。检索器按照相似度返回 top-$k$ 个文档：

$
  op("Retrieve")(q, cal(M), k)
  = arg max_((cal(S) subset [N], abs(cal(S)) = k))
    sum_(i in cal(S)) op("sim")(q, v_i)
$

其中，$op("sim")(dot, dot)$ 通常是余弦相似度。近似最近邻（ANN）索引，例如 FAISS、HNSW 和 ScaNN，可以让规模达到 $N approx 10^7$ 的检索变得可行。

*检索策略。* 常见的检索方式包括：

- *稠密检索：* 查询和文档都由神经编码器编码，例如 DPR 或 text-embedding-3-large。它能捕捉语义相似性，但通常需要 GPU 模型推理；
- *稀疏检索：* 使用基于 token 重叠的 BM25 或 TF-IDF，速度快、可解释，并且对精确关键词匹配很强；
- *混合检索：* 通过 Reciprocal Rank Fusion（RRF）组合稠密和稀疏分数：

$
  op("RRF")(d, k)
  = sum_(r in cal(R)) frac(1, k + op("rank")_r(d))
$

其中，$k = 60$ 通常作为平滑常数，$cal(R)$ 表示参与融合的检索器集合。

*重排序。* 初始检索返回较大的候选集后，可以使用 cross-encoder 重排器 $f_phi(q, d) in [0, 1]$ 对每个候选文档与查询联合打分，再只保留 top-$k$ 文档。这样可以用 $O(k')$ 次前向传播换取更高的精确率，其中 $k' >> k$。

#memory-box("检索幻觉风险")[

  RAG 并不能消除幻觉，反而可能引入新的错误。如果检索到的文档已经过时、不正确，或只是与查询表面相关，模型可能会自信地纳入错误信息。因此，记忆系统应始终保存来源元数据、时间戳和置信度，并在生成答案时考虑忠实性验证步骤。
]

=== 基于摘要的记忆

当逐字存储变得昂贵或噪声过大时，摘要会在存储前压缩信息。

*渐进式摘要#footnote[就是每来一次question和对应的answer，就将核心内容放到摘要当中，越滚越大]。* 在每个时间步 $t$，智能体维护一个滚动摘要 $S_t$。当新信息 $e_t$ 到达时，调用 LLM 合并旧摘要与新经验：

$ S_(t+1) = op("LLM")("Summarize: " + [S_t] + [e_t]) $

这样可以让记忆大小保持为 $O(1)$，但代价是可能丢失细节。

*层级压缩。* 将记忆组织成层级 $L_0 supset L_1 supset dots supset L_K$，其中 $L_0$ 保存逐字内容，每个 $L_(i+1)$ 都是 $L_i$ 的摘要。检索时先查看最压缩、最快的 $L_K$，再根据需要向下钻取原始内容。

*何时摘要，何时逐字存储。* 可以遵循以下经验规则：

- *逐字存储：* 精确事实、代码片段、数值结果和用户引述；
- *摘要：* 叙事上下文、推理链以及大量重复的观察；
- *丢弃：* 噪声、无信息量的失败工具调用和重复日志。

=== 基于图的记忆

*知识图谱。* 知识图谱 $cal(G) = (cal(V), cal(E), cal(R))$ 将事实存储为三元组 $(h, r, t)$，其中 $h$ 和 $t$ 是实体，$r$ 是关系。智能体可以通过 SPARQL、Cypher，或自然语言到图查询的翻译器访问它。

*实体—关系抽取。* 新观察可以由抽取模型解析为：

$ "text" arrow.r {(h_i, r_i, t_i)} $

随后将三元组与已有图谱合并，并执行实体链接和冲突消解，以确保同一实体不会被重复记录。

*GraphRAG。* #link("/xiaoxu/docs/technical-docs/llm/rag/")[GraphRAG] 用图遍历增强普通 RAG：给定查询，先找到种子实体，再扩展它们的 $k$-hop 邻域：

$
  op("GraphRetrieve")(q, cal(G), k)
  = union_(v in op("Seeds")(q)) cal(N)_k(v, cal(G))
$

其中，$cal(N)_k(v, cal(G))$ 表示图 $cal(G)$ 中实体 $v$ 的 $k$ 跳邻域。这样可以把 embedding 相似度难以直接匹配、但通过实体关系相连的事实带入上下文。

*时序知识图谱。* 对于随时间变化的事实，可以将有效区间写入三元组：$(h, r, t, [t_"start", t_"end"])$。时序知识图谱因此能够回答“2023 年 OpenAI 的 CEO 是谁？”这类问题，而不会把过去状态和当前状态混在一起。

=== 键值记忆网络

可微记忆网络将记忆表示为一组键值对 $\{(k_i, v_i)\}_(i=1)^M$，并用基于软注意力的检索读取记忆：

$
  alpha_i
  = op("softmax")(
    frac(q^top k_i, sqrt(D))
  ),
  quad
  c = sum_(i=1)^M alpha_i v_i
$

其中，$q$ 是当前查询，$k_i$ 是第 $i$ 个记忆键，$v_i$ 是与之关联的记忆值，$D$ 是键向量维度，$c$ 是读取到的上下文。由于整个读取过程可微，记忆槽可以通过梯度下降更新，也可以通过显式写入操作更新。

=== MemGPT

MemGPT 将记忆组织成多个层级，并把有限的上下文窗口视为一种需要主动管理的虚拟上下文。智能体根据以下因素决定哪些记忆被提升到热上下文（page-in），以及哪些记忆被逐出（page-out）：

- *近期性：* 最近访问的条目更可能再次需要；
- *相关性：* 与当前查询相似度更高的条目；
- *重要性：* 写入时被标记为更重要的条目。

这种设计使智能体能够在固定上下文窗口之外维护更大的长期记忆。更重要的是，LLM 自身可以把记忆管理函数（例如 `memory_search`、`memory_insert` 和 `memory_delete`）作为动作空间的一部分调用，使记忆管理成为一种可学习行为，而不是完全写死的规则。

== 记忆操作

=== 写入：提交到记忆

并不是每个观察都应该被存储。写入决策是一个过滤问题，可以用阈值表示：

$ op("Write")(e) = 1[op("importance")(e) > tau] $

其中，$tau$ 是写入阈值，$op("importance")(e)$ 可以由以下信号估计：

- *惊讶度：* $-log p_theta(e | "context")$，越意外的事件通常包含越多信息；
- *奖励信号：* 与 $abs(r_t)$ 相关的事件更值得记忆，无论它带来正向还是负向结果；
- *LLM 自评：* 提示模型按照 1—10 的尺度评估一条观察的重要性。

=== 冲突检测

在写入新事实 $f_"new"$ 之前，先检查它是否与已有记忆冲突：

$ op("Conflict")(f_"new", cal(M)) = exists f in cal(M): op("Contradicts")(f_"new", f) $

冲突检测可以通过 NLI 模型实现，也可以通过提示 LLM 完成。发现冲突时，智能体需要决定：覆盖旧事实、带时间戳同时保留两者，或将其标记为需要人工审查。

=== 记忆格式和粒度

除了存储什么，还需要决定以何种粒度存储记忆。记忆条目的范围可以从原子事实到长转录文本，不同选择具有不同权衡：

#table(
  columns: (1.1fr, 1.6fr, 1.6fr),
  align: (left, left, left),
  inset: 8pt,
  stroke: 0.6pt + luma(70%),
  table.header(
    [*格式*],
    [*优点*],
    [*缺点*],
  ),
  [*原子事实*\
  “用户偏好 Python。”],
  [检索精确；容易组合；不依赖长上下文],
  [抽取可能出错；容易丢失上下文；需要处理矛盾],
  [*结构化笔记*\
  （A-MEM）],
  [元数据丰富（标签、链接）；支持图遍历；平衡精确性和上下文],
  [写入成本更高；需要设计 schema；维护上下文关系更复杂],
  [*摘要情节*\
  （MemGPT）],
  [保留叙事连贯性；紧凑；适合多轮推理],
  [摘要可能有损；难以局部更新；细节可能被压缩],
  [*逐字转录*],
  [无损；没有抽取错误；支持精确引用],
  [存储量大；检索噪声高；扫描成本高],
)

在实践中，生产系统通常会组合多种粒度：抽取原子事实以便精确问答，维护摘要情节以保持叙事上下文，并把原始“memory objects”作为可追溯的底层证据保存。

=== 设计准则

- *让粒度匹配查询类型：* 如果用户问事实问题（“我的 API key 是什么？”），原子事实更有优势；如果用户问上下文问题（“我们为什么决定使用 Redis？”），则需要情节摘要。
- *在可承受的范围内按最细粒度存储：* 然后在其上构建更粗粒度的视图。从原子事实生成摘要很容易，从有损摘要还原原始事实则不可能。
- *包含来源：* 每个记忆条目都应链接回其来源（对话轮次、文档或工具输出），这样智能体可以验证，用户也可以审计。

=== 读取/检索
查询构造。检索查询q不一定要是原始观察。更好的策略可以参考RAG当中提到的#footnote[就当复习了]：
1. HyDE：生成一个假设答案，对其做embedding，并把该embedding作为查询
2. 查询拓展：生成查询的多个改写，并取检索结果的并集
3. Step-back prompt：在检索前，把具体的查询抽象成更一般的问题#footnote[比如问openai最大的竞争对手是谁，回退一步就是问openai是什么]

时间衰减和近期偏置。

较旧的记忆可能与当前查询不那么相关，因此可以在语义相似度之外加入近期性权重。一个按时间加权的检索分数可以写成：

$
  op("score")(d, q, t)
  = lambda op("sim")(q, v_d)
  + (1 - lambda) op("exp")(-frac(t - t_d, tau_"decay"))
$

其中，$t_d$ 是记忆 $d$ 的创建时间，$t$ 是当前检索时间，$tau_"decay"$ 控制时间衰减速度。$tau_"decay"$ 越小，系统越偏好近期记忆；$tau_"decay"$ 越大，旧记忆的影响衰减得越慢。Generative Agents 论文也使用了类似的近期性加权检索。

=== 冲突解决和巩固

记忆巩固会合并相关记忆，以减少冗余，并提炼出更高层的模式：

$
  cal(M)' = op("Consolidate")(cal(M))
  = op("Cluster")(cal(M)) union
    op("Summarize")(op("Cluster")(cal(M)))
$

巩固机制可以把多个相似的记忆聚类，再将每个簇压缩成一条更抽象的摘要。这样既能节省存储空间，也能减少检索时的重复信息。

遗忘机制。生物记忆会遗忘，人工记忆也应该如此。常见策略包括：

- *LRU 逐出：* 当容量超限时，移除最近最少使用的条目；
- *按重要性加权遗忘：* 重要性越低的记忆越容易被遗忘：

  $ p("forget" | d) prop op("exp")(-op("importance")(d)) $

- *间隔重复：* 被反复访问的记忆保留更久，遵循指数遗忘曲线。

=== 反思：元认知操作

反思（Reflection）是一种更高阶的记忆操作：智能体读取自己的记忆，并从多个情节记忆中抽象出新的洞见：

$ op("Reflect")(cal(M)) arrow.r {i_1, i_2, dots} subset cal(M)_"semantic" $

其中，每个洞见 $i_j$ 都是从多个情节记忆中提炼出的更高层抽象，而不是某一条具体事件的简单复制。反思可以把“发生过什么”进一步转化为“应该如何做”，从而帮助智能体在后续任务中复用经验。

#memory-box("实践中的反思（Reflection）")[

  假设智能体连续三次尝试解决一个编码问题都失败了，它可以进行如下反思：

  1. 从情节记忆中检索三条失败情节；
  2. 生成一个洞见：“我总是忘记处理输入列表为空的边界情况。”；
  3. 将这个洞见写入语义记忆；
  4. 下一次尝试时，检索该洞见，并显式检查空输入。

  这是 Reflection 的核心机制：通过自我反思把具体经历转化为可迁移的语言化规则，强化后续任务中的行为选择。
]

== 多轮对话中的记忆

=== 用户建模和偏好跟踪

持久用户模型 $U$ 会存储与用户长期交互相关的信息#footnote[比如现在gpt网页端你打开记忆模式，你能看到他对于你最近人物画像的总结]：

- *显式偏好：* 用户明确表达的喜欢或不喜欢，以及沟通风格偏好；
- *隐式偏好：* 从行为中推断出的偏好，例如用户总是要求使用 Python 代码、偏好简洁答案；
- *专业水平：* 从词汇和问题复杂度推断出的领域知识水平；
- *目标和上下文：* 正在进行的项目、当前任务和用户在组织中的角色。

用户模型会在每次交互后更新：

$ U_(t+1) = op("Update")(U_t, (u_t, a_t, op("feedback")_t)) $

其中，$u_t$ 是用户输入，$a_t$ 是智能体的回答，$op("feedback")_t$ 表示用户显式或隐式提供的反馈。

=== 会话连续性

没有记忆时，每次对话都会从零开始；有了会话记忆后，智能体可以在多轮交互之间保持连续性：

1. 在会话开始时，检索用户模型 $U$ 和近期会话摘要；
2. 将个性化信息注入系统 prompt，例如：“你正在帮助 Alice，她是一名资深 ML 工程师，正在做一个分布式训练项目。上次会话中，你帮助她调试了一个梯度同步问题。”；
3. 在会话结束时，总结本次会话并更新 $U$。

这样，下一次会话就不再是完全冷启动，而是可以从用户已有的长期背景和近期任务状态继续工作。

=== 通过记忆实现个性化

个性化可以同时提升效率（减少澄清问题）和质量（回答与用户专业水平匹配）。关键技术包括：

- *自适应详略程度：* 根据用户历史参与情况调整回答长度；
- *领域预置：* 从语义记忆中前置相关领域上下文；
- *主动回忆：* 在用户尚未明确提出时，主动带出相关的过去交互，例如：“你上个月问过这个主题；这是我们当时发现的内容。”

== 多智能体系统中的记忆

当多个智能体协作完成共享任务时，记忆会成为一种协调机制，而不只是个人知识存储。负责规划的智能体在分解任务时必须把子目标传给执行智能体；批评智能体必须访问与被评估智能体相同的上下文；由多个智能体组成的研究团队必须避免重复工作。没有共享记忆，智能体必须通过直接消息传达所有内容，容易造成带宽瓶颈，并在对话中丢失信息。共享记忆提供了一个所有智能体都能读写的底座，使协作协议从隐式协同变成显式状态。

=== 共享记忆池

在多智能体系统中，每个智能体可以在私有存储 $cal(M)_i$ 之外共享一个公共记忆存储 $cal(M)_"shared"$：

$ op("context")_i(t) = cal(R)(cal(M)_i, q_i) union cal(R)(cal(M)_"shared", q_i) $

共享记忆支持隐式协调：智能体 $A$ 写入一个发现，智能体 $B$ 无需显式通信就能检索到它。

=== 黑板架构

黑板模式是一种经典的多智能体协调机制：每个智能体都从黑板读取信息并向黑板写入信息。控制器监控黑板，并在智能体前置条件满足时激活它们。这解耦了智能体：它们通过共享状态通信，而不是直接发送消息。

=== 共享知识中的共识和冲突

当多个智能体写入共享记忆时，冲突不可避免。常见解决策略包括：

- *最后写入胜出：* 规则简单，但可能丢失信息；
- *版本化记忆：* 维护所有写入的历史，智能体可以查询任意版本；
- *投票／共识：* 要求 $n$ 个智能体中的 $k$ 个同意，事实才能提交；
- *按置信度加权合并：* $f_"merged" = sum_i w_i f_i$，其中 $w_i$ 是智能体 $i$ 的置信度；
- *指定权威：* 将记忆区域的所有权分配给特定智能体。

#memory-box("开放问题：分布式记忆一致性")[

  多智能体系统应如何在发生并发写入、网络分区和对抗性智能体的情况下维护记忆一致性？经典分布式系统方案（Paxos、Raft）适用，但成本很高。对于许多智能体任务，带有界限性的近似一致性可能已经足够，但正确的权衡仍然是一个开放研究问题。
]

== 用强化学习训练记忆系统

=== 记忆操作的奖励信号

记忆操作（读取、写入、更新、反思）可以被视为强化学习框架中的动作。难点在于设计奖励信号，以激励有用的记忆行为：

- *任务奖励传播：* 如果一次记忆检索带来了正确答案，就把功劳分配给检索动作。信号稀疏，但含义明确；
- *检索精度奖励：* $r_t^"retrieve" = op("Relevance")(d_"retrieved", "task")$，由学习到的相关性模型估计；
- *记忆效率奖励：* 惩罚不必要的写入，例如 $r_t^"write" = -lambda 1["write"]$，鼓励选择性存储；
- *一致性奖励：* 奖励内部一致、没有明显矛盾的记忆状态。

步骤 $t$ 的记忆操作 $m_t$ 可以使用组合奖励：

$
  r_t
  = r_t^"mem"
  + alpha dot r_t^"task"
  + beta dot r_t^"retrieve"
  + gamma dot r_t^"write"
  + gamma dot r_t^"consistency"
$

其中，各个系数控制任务表现、检索质量、写入成本和记忆一致性在总奖励中的权重。

=== 学习记忆在什么时候写入

记忆写入问题是一个元学习挑战：智能体必须学习一个写入策略 $pi_"write"(e)$，以最大化未来任务表现。这很困难，原因包括：

1. 记忆的价值可能要到未来才会显现，写入时只能得到延迟奖励；
2. 写入时通常不知道未来会查询什么；
3. 记忆会相互作用：存储事件 $e$ 的价值取决于记忆库 $cal(M)$ 中已经存在什么。

常见方法包括：

- *事后重标注：* 在一次成功回合结束后，回溯性地把被检索到的记忆标记为“重要”，并训练写入策略存储相似条目；
- *Meta-RL：* 在一组任务上训练写入策略，使策略学会存储能够跨任务泛化的信息；
- *好奇心驱动存储：* 存储令人惊讶的观察，也就是预测误差较高、可能包含更多信息量的事件。

=== 记忆增强的策略优化

可以联合优化智能体策略与记忆系统。记忆增强策略优化的目标函数可以写成：

$
  cal(L)(theta, phi)
  = E_(tau ~ pi_theta)[sum_(t=0)^T gamma^t r_t]
  - lambda cal(L)_"mem"(phi)
$

其中，$theta$ 是 LLM 的参数，$phi$ 是记忆系统的参数（例如检索器的权重），$cal(L)_"mem"(phi)$ 是对记忆复杂度或记忆操作成本的正则项。这个目标让模型同时考虑任务回报和记忆系统的开销。

#memory-box("关键洞见：记忆作为可学习的归纳偏置")[

  用强化学习训练记忆操作，可以让智能体发展出面向任务的记忆策略。编码智能体会学会存储 API 签名；研究智能体会学会存储引用；客服智能体会学会存储用户投诉模式。记忆系统因此成为一种为智能体领域量身定制的可学习归纳偏置。
]

== 评估记忆系统

评估智能体记忆颇具挑战性，因为记忆操作的质量往往要经过较长时间，才能从智能体的整体表现中间接体现。一个能够完美回忆事实的系统，仍然可能因为检索了不相关的上下文，或压垮了 LLM 的上下文窗口而失败。

=== 评估维度

长期记忆评估需要覆盖以下五项核心能力：

1. *信息抽取：* 系统能否从对话轮次中识别并存储显著事实？用事实召回率衡量：真实事实中有多少可以从记忆中恢复？
2. *多会话推理：* 系统能否综合分散在多个过去会话中的信息？例如：“根据我们上周和昨天的对话，项目范围发生了什么变化？”
3. *时序推理：* 系统能否正确回答依赖时间的查询？例如：“在重组之前，我说过我的优先事项是什么？”这要求系统区分不同时间点的状态。
4. *知识更新：* 当事实发生变化时，例如用户搬家或偏好改变，记忆能否反映最新状态，同时保留必要的历史？
5. *拒答：* 当系统没有相关记忆时，能否正确地说“我不知道”，而不是幻觉式地编造一个看似合理的回忆？

=== 基准

#table(
  columns: (1.15fr, 1fr, 1.35fr, 1.8fr),
  align: (left, left, left, left),
  inset: 8pt,
  stroke: 0.6pt + luma(70%),
  table.header(
    [*基准*],
    [*会议*],
    [*规模*],
    [*关注点*],
  ),
  [*LongMemEval*], [ICLR 2025], [500 个问题，可扩展历史], [五项记忆能力；多会话聊天],
  [*LOCOMO*], [EMNLP 2024], [多会话对话], [对话上的单跳、时序、多跳和开放域问答],
  [*InfiniteBench*], [ACL 2024], [100K+ token 上下文], [长上下文回忆；并非记忆专用，但可以测试上下文极限],
)

=== 指标

*记忆级指标。* 这些指标直接评估记忆存储与检索本身：

- *Memory Recall：* 衡量真实事实中有多少可以被记忆系统找回：

  $ op("Memory Recall") = frac(N_"ground-truth facts retrievable from memory", N_"total ground-truth facts") $

- *Memory Precision：* 衡量 top-$k$ 检索结果中的记忆有多少与查询相关：

  $ op("Memory Precision") = frac(N_"relevant items in top-k retrieval", k) $

- *延迟：* 从查询到检索上下文所需的时间，通常报告 p50 和 p95；
- *Token 效率：* 每次查询注入上下文的总 token 数。越低越好，因为不必要的上下文会降低 LLM 的准确率并增加成本。

*下游指标。* 这些指标评估记忆对最终任务的帮助：

- *答案准确率：* 以记忆为条件时最终响应的正确性，可以使用 EM、F1 或 LLM-as-judge；
- *忠实性：* 响应是否准确反映记忆中的内容，而不是编造；
- *个性化质量：* 用户满意度，或通过偏好评分、记忆增强系统与无记忆系统之间的 A/B 测试衡量；
- *矛盾率：* 系统生成与先前陈述事实不一致的响应所占比例。

*运行指标。* 这些指标衡量记忆系统长期运行的成本与稳定性：

- *写入选择性：* 触发记忆写入的轮次比例。过高意味着噪声，过低意味着信息缺口；
- *陈旧性：* 尽管记忆正在更新，但仍然检索到过时事实的频率；
- *存储增长率：* 每次交互新增的 token 数。无界增长不可持续。

#memory-box("评估缺口")[

  大多数记忆论文在短基准（10—50 个会话）上评估。真实生产智能体会运行数月，并拥有数千个会话。长期评估仍是开放挑战；在这种评估中，记忆漂移、矛盾累积和存储膨胀会成为主要失败模式。实践者应当对运行指标做长期持续追踪，以补充基准分数。
]

== 总结

智能体记忆系统是高能力 AI 智能体的基础组件，用来应对有限上下文窗口这一根本限制。本文概览了：

- *四分法分类：* 工作记忆、情节记忆、语义记忆和程序性记忆，对应认知科学中的区分，也对应不同的工程需求；
- *五类架构家族：* 基于 RAG、基于摘要、基于图、键值记忆网络，以及分层虚拟上下文（MemGPT）；
- *四种核心操作：* 写入（包含重要性评分和冲突检测）、读取/检索（包含时间衰减和查询扩展）、更新（包含冲突解决和巩固），以及反思（元认知洞见生成）；
- *多轮和多智能体扩展：* 用户建模、会话连续性、共享记忆池和黑板架构；
- *记忆系统的强化学习训练：* 奖励信号、学习记忆何时写入，以及记忆增强的策略优化。

这一领域正在快速发展。关键开放挑战包括：

1. *记忆落地：* 确保检索到的记忆真正被智能体用于生成，而不是被忽略或被新的幻觉覆盖；
2. *可扩展一致性：* 在大规模多智能体系统中维护共享记忆，同时控制同步成本；
3. *隐私保护：* 在使用用户数据实现个性化的同时，避免不必要的数据暴露和不当推断。

随着上下文窗口不断增长，工作记忆与外部记忆之间的边界会继续变化；但选择性、结构化、可搜索的长期存储仍然是构建可靠智能体的核心。
